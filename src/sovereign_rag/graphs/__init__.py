"""LangGraph workflows.

Each subpackage under ``graphs/`` is one graph: a folder with ``graph.py``
(exposing an async ``make_graph()`` factory), ``state.py``, and ``nodes.py``.

Registered graphs are listed in ``langgraph.json`` at the repo root.
"""
