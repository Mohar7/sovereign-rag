# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Virtual envs
.venv/
venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
junit.xml
htmlcov/

# Secrets
.env
.env.*
!.env.example

# Local model caches / artifacts
.flashrank_cache/
*.onnx
models/

# Docling / crawl4ai caches
.docling_cache/
.crawl4ai/

# Runtime state managed by the live deploy (pin/exclude JSON, deploy marker)
.runtime/
.deploy-sha

# SearXNG runtime state — limiter db, uwsgi caches, etc. Our committed
# settings.yml stays tracked (small overrides on top of upstream defaults).
deploy/searxng/limiter.toml
deploy/searxng/*.db
deploy/searxng/uwsgi.ini

# LangGraph dev-server runtime checkpoint cache (in-memory store snapshots).
.langgraph_api/

# Local tool config (Claude Code workspace + cached skills).
.agents/
.claude/
skills-lock.json

# Downloaded Claude Design bundle — local design reference only, not vendored.
# Regenerable from docs/crag-hitl-design-prompt.md. The CRAG spec references
# design/Sovereign RAG Design System/crag/ as a local implementation reference.
design/

# IDE / OS
.idea/
.vscode/
.DS_Store
.envrc

# Local-only ingest corpus + runtime progress (mkbank assistant)
docs/context-docs/
.ingest-context-progress.json
