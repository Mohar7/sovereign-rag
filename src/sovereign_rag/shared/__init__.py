"""Cross-cutting utilities used by every graph.

- ``llm_factory`` — tiered chat-model factory (``get_chat_model``)
- ``tracing`` — Langfuse callback handler
- ``pipeline_deps`` — process-wide RAGPipeline singleton accessor
"""
